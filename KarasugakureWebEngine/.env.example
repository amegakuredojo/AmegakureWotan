# Copia este archivo a .env y completa. .env NUNCA se commitea (añádelo a .gitignore).

# Brave Search API — https://api-dashboard.search.brave.com/
BRAVE_API_KEY=

# Se autogenera con openssl si lo dejas vacío (ver deploy.sh)
SEARXNG_SECRET=
