[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "karasugakure"
version = "0.1.0"
description = "CLI-only OSINT orchestration harness"
requires-python = ">=3.11"
dependencies = [
  "typer>=0.12",
  "rich>=13.7",
  "textual>=0.70.0",
  "pydantic>=2.7",
  "pydantic-settings>=2.3",
  "PyYAML>=6.0",
  "langgraph>=0.2",
  "kuzu>=0.4.0",
  "mcp>=1.0.0",
  "httpx[socks]>=0.27",
  "requests[socks]>=2.32",
  "PySocks>=1.7.1",
  "beautifulsoup4>=4.12",
  "lxml>=5.2",
  "selenium>=4.21",
  "python-gnupg>=0.5.2",
  "anyio>=4.4",
  "tldextract>=3.1",
  "stem>=1.8.1",
  "smolagents>=1.0.0",
  "langchain>=0.2.0",
  "langchain-community>=0.2.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.2",
  "pytest-asyncio>=0.23",
  "pytest-cov>=5.0",
  "freezegun>=1.5",
]

[project.scripts]
karasu = "karasugakure.cli:app"
amewotan = "karasugakure.cli_wotan:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
